/**
 * Representa la cola de sincronización offline.
 */
export class SyncQueue {
  async enqueue(item: unknown): Promise<void> {
    // Placeholder: encolar elementos de sincronización.
  }

  async dequeue(): Promise<unknown | null> {
    return null;
  }
}
