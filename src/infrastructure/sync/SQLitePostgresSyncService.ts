export class SQLitePostgresSyncService {
  async sync(): Promise<void> {
    // Placeholder para la sincronización entre la base local SQLite y PostgreSQL.
    // Esta clase debe permanecer independiente de React y de la UI.
  }
}
