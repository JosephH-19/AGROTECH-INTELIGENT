/**
 * SyncEngine coordina la sincronización offline-first.
 * Se mantiene desacoplado de la UI y del dominio.
 */
export class SyncEngine {
  async start(): Promise<void> {
    // Placeholder: iniciar trabajos de sincronización.
  }
}
